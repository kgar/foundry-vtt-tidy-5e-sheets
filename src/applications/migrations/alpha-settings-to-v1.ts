import { CONSTANTS } from 'src/constants';

export function migrateAlphaSettingsToV1() {
  // get settings from game.settings.storage
  // Find all GM settings with the `tidy5e-kgar` namespace
  // Get all GM settings with the `tidy5e` namespace

  // Map the alpha settings
  // Remove any OG settings that are not in the list
  // Log every change
}
