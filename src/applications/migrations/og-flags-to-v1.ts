export function migrateOgFlagsToV1() {
  // Flags to update
  const flagDifferences = [
    {
      v1: 'skillsExpanded',
      og: 'npcSkillsExpanded',
    },
  ];

  // Go through each NPC sheet that is editable

  // Get flag value

  // Create diff update that removes the old flag and adds the new one.

}
